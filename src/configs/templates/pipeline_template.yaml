pipeline:
  name: string
  description: string
  schedule: string
  start_date: string
  catchup: boolean
  retries: integer

  source: 
    type: string
    connection_id: string
    schema: string
    table: string

  transformations:
    - type: string
      params: dict

  target:
    type: string
    connection_id: string
    schema: string
    table: string
    write_mode: string